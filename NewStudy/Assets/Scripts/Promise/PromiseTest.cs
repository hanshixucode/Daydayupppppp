using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PromiseTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //todo hsx promise Asyn
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
